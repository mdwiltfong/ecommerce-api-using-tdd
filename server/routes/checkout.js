const express = require("express");
const checkoutRouter = express.Router();

// Increment shopping cart item
checkoutRouter.put("/plus", async (req, res, next) => {
   
});


module.exports = checkoutRouter;
